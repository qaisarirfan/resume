const styles = theme => ({
  toolbar: {
    // flexDirection: "column",
    // justifyContent: "center",
    // alignItems: "flex-start"
  },
  profile: {
    width: "48px",
    marginRight: "12px",
    overflow: "hidden",
    borderRadius: "50%",
    height: "48px",
    "& img": {
      width: "100%"
    }
  }
})

export default styles
